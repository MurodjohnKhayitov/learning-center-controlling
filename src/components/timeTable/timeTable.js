import React from 'react';
import PropTypes from 'prop-types';

const timeTable = () => {
    return (
        <div>
            
        </div>
    );
};

timeTable.propTypes = {};

export default timeTable;